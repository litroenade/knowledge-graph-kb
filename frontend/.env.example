# Frontend dev server reference.
# This file is not loaded by Vite. Runtime overrides come from `frontend/.env` or shell environment variables.

KB_FRONTEND_API_PROXY_TARGET=http://127.0.0.1:8000
KB_FRONTEND_DEV_HOST=
KB_FRONTEND_DEV_PORT=
